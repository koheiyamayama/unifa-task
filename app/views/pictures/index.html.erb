<%= link_to 'ログアウト', 'sessions', method: :delete %>
<h1>写真一覧</h1>
<%= link_to '写真アップロード', new_picture_path %>
<div style="display: flex; flex-wrap: wrap;">
<% @pictures.each do |picture| %>
  <div style="width: 30%;">
    <%= picture.title %>
    <%= image_tag(picture.file) %>
  </div>
<% end %>
